{% extends 'base.html.twig' %}

{% block title %}{{ 'edit_category_name' |trans  }}{% endblock %}

{% block body %}
<div style="margin-left: 44%; margin-top: 8%;">
    {% for message in app.flashes('success') %}
        <div class="flash">
            {{ message }}
        </div>
    {% endfor %}
</div>
    <h3 style="color:purple; margin-left: 44%; margin-top: 8%;">{{ 'edit_category_name' |trans  }}</h3>

    {{ include('categories/_form.html.twig', {'button_label': 'ZAPISZ'}) }}
    <div style="margin-left: 51%; margin-top: 1%">
        {{ include('categories/_delete_form.html.twig') }}
    </div>

{% endblock %}
