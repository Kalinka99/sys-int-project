<form method="post" action="{{ path('comments_delete', {'id': comment.id}) }}" onsubmit="return confirm('Do you really want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.id) }}">
    <button class="btn">{{ 'delete_button'|trans }}</button>
</form>
